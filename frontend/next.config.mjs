/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["evento-s3bucket.s3.us-east-1.amazonaws.com",'ui-avatars.com','lh3.googleusercontent.com', 'res.cloudinary.com'],
    },
  };
  
  export default nextConfig;
  